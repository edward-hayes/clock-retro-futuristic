"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const memory_profiler_1 = require("@fitbit/memory-profiler");
const fs_extra_1 = tslib_1.__importDefault(require("fs-extra"));
async function captureHeapSnapshot(host, format, destPath, uuid, sourceMaps) {
    let v8Requested = false;
    let actualFormat = format;
    if (format === 'v8') {
        v8Requested = true;
        actualFormat = 'jerryscript-1';
    }
    if (!host.getHeapSnapshotSupport().supported) {
        throw new Error('Connected device does not support heap snapshots');
    }
    const snapshot = await host.captureHeapSnapshot(actualFormat, uuid);
    if (!v8Requested) {
        return fs_extra_1.default.writeFile(destPath, snapshot);
    }
    const graph = await (0, memory_profiler_1.generateGraph)(snapshot, actualFormat, sourceMaps || {});
    const v8Snapshot = (0, memory_profiler_1.generateV8HeapSnapshot)(graph);
    return fs_extra_1.default.writeFile(destPath, JSON.stringify(v8Snapshot));
}
exports.default = captureHeapSnapshot;
//# sourceMappingURL=captureHeapSnapshot.js.map